install the front end dependencies 
the prject uses fastapi , uvicorn , react, mlflow
to begin run in terminal mlflow server
followed by uvicorn app:app --reload
fianlly starting the front end by npm run dev
